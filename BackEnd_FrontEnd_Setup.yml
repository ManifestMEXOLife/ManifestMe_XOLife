mkdir manifestme-backend && cd manifestme-backend
npm init -y
npm install express cors dotenv pg reflect-metadata typeorm bcrypt jsonwebtoken
npm install -D typescript ts-node-dev @types/express @types/node @types/jsonwebtoken @types/bcrypt
npx tsc --init
