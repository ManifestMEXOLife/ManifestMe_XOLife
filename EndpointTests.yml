# API Endpoint Tests
# This file has been converted to EndpointTests.sh
# Use the shell script to run the actual curl commands

endpoints:
  auth:
    register: "POST /api/auth/register"
    login: "POST /api/auth/login"
  goals:
    create: "POST /api/goals"
    create_micro: "POST /api/goals/micro"
  videos:
    request: "POST /api/videos/request"